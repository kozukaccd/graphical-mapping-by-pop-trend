import React, { useEffect, Fragment } from "react";

// import { useRESAS } from "~/hooks/useRESAS";

const GraphView: React.FC = () => {
  // const { graphData } = useRESAS();

  return <Fragment></Fragment>;
};

export default GraphView;
